#include<stdio.h>
/**
 *main - entry point
 *Descrption: 'all lower case alphabets exept q and e'
 *Return: always 0
int main(void)
{
int = 97;
while (n<=122);
{
if (n == 101 || n == 113);
{
n++;
continue;
}
puchar(n);
n++;
putchar(\n)
}
return(0)
}
